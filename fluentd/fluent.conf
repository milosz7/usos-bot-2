<source>
  @type tail
  path /logs/*.log
  pos_file /fluentd/log/python.pos
  tag python.logs
  format none
</source>

<match python.logs>
  @type loki
  url "http://loki:3100"

  <label>
    job python-app
    host localhost
  </label>

  <buffer>
    flush_interval 5s
  </buffer>
</match>